name: Gradle CI

on:
  push:
    branches: [ main, master, task-2-1 ]
  pull_request:
    branches: [ main, master, task-2-1 ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Grant execute permission for gradlew
        run: chmod +x task_2_1/gradlew

      - name: Build with Gradle
        run: task_2_1/gradlew clean build --info
        working-directory: task_2_1

      - name: Run Checkstyle
        run: task_2_1/gradlew checkstyleMain checkstyleTest
        working-directory: task_2_1
